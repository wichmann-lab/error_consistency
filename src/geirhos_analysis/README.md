
## Revisiting Robert's data

In this analysis, we take a new look at the data from Robert's closing-the-gap-paper.

1. run `aggregate_roberts_data.ipynb`, should write to `data/roberts_raw_data.parquet`
2. run `bootstrap.py`
3. run `plot_roberts_data.py` 